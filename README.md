# MyTube – фронтенд стримингового сервиса

Интерфейс для современного видеосервиса с акцентом на фильмы, аниме и удобную аналитику просмотра. Лёгкий UI, прозрачная архитектура, готовность к работе с продакшен-бэкендом на JWT.

---

## 🎯 Концепция

MyTube — это SPA на React, которое:

* показывает каталог фильмов и аниме;
* позволяет искать, фильтровать и сортировать контент;
* поддерживает избранное, рейтинги и историю просмотров;
* интегрируется с backend API (Spring Boot) через JWT;
* даёт пользователю базовую аналитику по просмотрам.

Фронт не играет «сам за себя», он заточен под связку с REST API и сразу строится так, чтобы не превратиться в кашу при росте проекта.

---

## 🚀 Основные возможности

### Каталог

* Список фильмов и аниме в виде аккуратных карточек:

  * постер;
  * название;
  * год;
  * жанры;
  * базовый рейтинг.
* Поддержка пагинации или lazy-load (в зависимости от реализации API).
* Единый светлый, аккуратный стиль плиток:

  * светлая подложка;
  * тонкая рамка;
  * лёгкие скругления;
  * плавный hover без цирка.

### Поиск и фильтрация

* Строка поиска по названию.
* Подсветка совпадений в результатах (highlight).
* Фильтры:

  * по жанрам;
  * по типу (фильм / аниме), если поддерживается API;
  * по году / рейтингу (опционально).
* Быстрая комбинация: поиск + фильтры работают вместе.

### Страница фильма

* Детальная информация:

  * название, год, описание;
  * жанры many-to-many;
  * режиссёр, актёры;
  * постер, базовые рейтинги.
* Действия пользователя:

  * ⭐ поставить/обновить рейтинг;
  * 📝 оставить комментарий (если реализовано на бэке);
  * ❤️ добавить / убрать из watchlist («Буду смотреть»).
* Место под плеер:

  * интеграция с видео-источником (YouTube/файлы/стрим) через `videos`-API;
  * отправка beat-событий для истории просмотра (watch history).

### Watchlist (избранное / «Смотреть позже»)

* Список добавленных пользователем тайтлов.
* Базовые операции:

  * добавить в watchlist прямо из карточки;
  * удалить одним кликом;
  * переход на карточку фильма.

### Рейтинги

* Возможность поставить оценку фильму (например 1–10).
* Проверка «один пользователь — один рейтинг на фильм».
* Отображение среднего рейтинга (если отдаётся с сервера).
* Синхронизация с backend `ratings` API.

### История просмотров и аналитика

Интеграция с backend-моделью `watch_history`:

* Отправка `watch-history/beat`:

  * `movieId`, `sessionId`, `deltaSec`, `paused` и т.п.
* Страница «Моя статистика»:

  * ⏱ общее время просмотра;
  * 📊 распределение по жанрам (pie / bar);
  * 📅 активность по дням (line chart / area).

UI строится поверх `AnalyticsSummary`-ответа с бэкенда (totalSeconds, genresPie, activityByDay).

### Авторизация и роли

* JWT-аутентификация:

  * форма логина;
  * отправка логина/пароля на `/auth/login` (или аналог);
  * сохранение токена (localStorage / sessionStorage).
* Использование токена в защищённых запросах.
* Разделение зон:

  * публичные страницы (каталог, деталка);
  * личный кабинет / аналитика;
  * админ-панель (ролевая проверка, если приходит из токена).

### Админ-панель

Базовый UI для работе с контентом (подключается к Spring Boot API):

* Создание / редактирование фильмов.
* Управление жанрами.
* Привязка видео/субтитров (если реализовано в API).
* Просмотр и модерация пользовательского контента (при необходимости).

### UI / UX

* Светлая тема по умолчанию.
* Адаптивная вёрстка:

  * mobile-first;
  * сетка карточек на desktop;
  * аккуратные отступы.
* Карточки:

  * `background: #fafafa;`
  * `border: 1px solid #e5e5e5;`
  * `border-radius: 8px;`
  * `transition: 0.2s ease;`
  * лёгкий hover: небольшое поднятие и мягкая тень.

---

## 🛠️ Стек технологий

Используется современный, минимально необходимый набор:

* **React 18** — основа приложения.
* **Vite** — быстрый dev-сервер и сборка.
* **React Router DOM** — роутинг:

  * каталог,
  * детальная страница,
  * watchlist,
  * профиль/аналитика,
  * админ.
* **CSS3 (Grid / Flexbox)**:

  * layout для каталогов, панелей, карточек.
* **JWT**:

  * авторизация;
  * передача токена в `Authorization: Bearer ...`.
* Любой клиент для HTTP:

  * `fetch` или `axios` (по проекту).

Если используется TypeScript или state-менеджер (Redux / Zustand / RTK Query) — выносится в отдельный блок, но базовая идея: структура предсказуемая, без монстра из побочек.

---

## 📂 Структура проекта (рекомендованный вариант)

Примерная структура `src/`:

```text
src/
  api/             # функции для работы с backend API
  components/      # переиспользуемые UI-компоненты (Card, Layout, Button, etc.)
  pages/           # страницы (Home, MoviePage, WatchlistPage, AnalyticsPage, AdminPage)
  features/        # логика сущностей (auth, movies, watchlist, ratings, analytics)
  hooks/           # кастомные хуки
  styles/          # глобальные стили, темы, переменные
  router/          # конфигурация маршрутов
  utils/           # хелперы
  main.jsx
  App.jsx
```

Главные принципы:

* Компоненты маленькие.
* API-логика вынесена из компонентов.
* Чёткое разделение: «страница» против «глупых компонентов».

---

## ⚙️ Настройка и запуск

### 1. Клонирование

```bash
git clone <repository-url>
cd my-tube-app
```

### 2. Установка зависимостей

```bash
npm install
# или
yarn
```

### 3. Переменные окружения

Создайте `.env` или `.env.local`:

```env
VITE_API_URL=http://localhost:8080/api      # базовый URL backend-сервиса
VITE_JWT_STORAGE_KEY=mytube_token           # ключ для хранения токена
```

При необходимости:

* ключи для аналитики,
* флаги фич.

### 4. Запуск в dev-режиме

```bash
npm run dev
```

Приложение будет доступно, как правило, по адресу:
`http://localhost:5173` (или тот, что покажет Vite).

### 5. Сборка продакшен-версии

```bash
npm run build
npm run preview
```

Содержимое `dist/` разворачивается за nginx / любой статику.

---

## 🔗 Интеграция с backend

Фронт ожидает от backend (Spring Boot):

* `/api/movies` — список фильмов;
* `/api/movies/{id}` — детали фильма;
* `/api/genres` — жанры;
* `/api/watchlists` — операции с избранным;
* `/api/ratings` — CRUD рейтингов;
* `/api/watch-history/beat` — приём heartbeats;
* `/api/analytics/me/summary` — агрегированная аналитика;
* `/api/auth/login`, `/api/auth/register` — аутентификация.

Формат может отличаться, но архитектура фронта строится так, чтобы адаптация к API была локализована в `api/`.

---

## 🧩 Дальнейшее развитие

Проект уже готов как каркас для учебного/боевого стриминга. Логичные следующие шаги:

* полноценная интеграция с JWT и ролями (user/admin);
* улучшение аналитики: топы по типу контента (фильмы / аниме / сериалы);
* история просмотра с продолжением с последнего места;
* skeleton-загрузчики и lazy-loading для тяжёлых страниц;
* темизация (light/dark) без ломки текущего дизайна;
* компоненты под `videos` и `subtitles`, когда backend будет это отдавать.

---
